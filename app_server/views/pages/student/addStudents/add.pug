.page-heading
    .page-title
        .row
            .col-12.col-md-6.order-md-1.order-first
                h3 Add Students
            .col-12.col-md-6.order-md-2.order-last
                nav(aria-label="breadcrumb").breadcrumb-header.float-start.float-lg-end
                    ol.breadcrumb
                        li.breadcrumb-item
                            a(href="/") Dashboard
                        li.breadcrumb-item.active(aria-current="page") Add Students 
    section.section
      .card
        .card-header
          h4.card-title Add a student
        .card-body
          .row
            .col-md-6
                .form-group 
                    label(for="matno") Matric Number 
                    input(type="text", class="form-control matno", id="matno", placeholder="Enter your matric number")

script.
  // Simple Datatable
  let table1 = document.querySelector('#table1');
  let dataTable = new simpleDatatables.DataTable(table1);